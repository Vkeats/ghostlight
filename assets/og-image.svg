<svg width="1200" height="630" viewBox="0 0 1200 630" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#0a0a0c"/>

  <!-- Subtle grid -->
  <defs>
    <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
      <path d="M 60 0 L 0 0 0 60" fill="none" stroke="rgba(0, 255, 157, 0.03)" stroke-width="1"/>
    </pattern>
  </defs>
  <rect width="1200" height="630" fill="url(#grid)"/>

  <!-- Logo text -->
  <text x="600" y="280" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="72" font-weight="700" letter-spacing="0.04em">
    <tspan fill="#e0e0e8">ghost</tspan><tspan fill="#ffaa00">light</tspan><tspan fill="#5a5a6e" font-size="48" font-weight="400">.tech</tspan>
  </text>

  <!-- Tagline -->
  <text x="600" y="340" text-anchor="middle" font-family="monospace" font-size="24" fill="#a8a8b8" letter-spacing="0.05em">
    Infrastructure for live theatre.
  </text>

  <!-- Accent line -->
  <rect x="500" y="380" width="200" height="2" fill="#ffaa00" opacity="0.6"/>

  <!-- Product hints -->
  <text x="600" y="440" text-anchor="middle" font-family="monospace" font-size="16" fill="#5a5a6e" letter-spacing="0.1em">
    THE GUILD · THE BOARD
  </text>

  <!-- Location -->
  <text x="600" y="580" text-anchor="middle" font-family="monospace" font-size="14" fill="#5a5a6e" letter-spacing="0.1em">
    VANCOUVER, BC
  </text>
</svg>
